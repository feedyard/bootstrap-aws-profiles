---
version: 2.1

orbs:
  terraform-test: feedyard/terraform@dev:latest

workflows:
  bootstrap-aws-profiles-iam-only-deploy:
    jobs:
      - terraform-test/plan:
          account: profile
          env: profile