---
version: 2.1

orbs:
  terraform: feedyard/terraform@dev:latest

workflows:
  bootstrap-aws-profiles-iam-only-deploy:
    jobs:
      - terraform/plan:
          account: profile
          env: profile